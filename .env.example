# Database
# For PostgreSQL (Docker):
DATABASE_URL="postgresql://maithili:maithili123@localhost:5432/maithili_dict"
# For PostgreSQL (Manual installation):
# DATABASE_URL="postgresql://username:password@localhost:5432/maithili_dict"
# For SQLite (Quick start):
# DATABASE_URL="file:./dev.db"

# Direct database URL for migrations (same as DATABASE_URL for most cases)
DIRECT_URL="postgresql://maithili:maithili123@localhost:5432/maithili_dict"

# Redis (optional for local dev - can skip if not using Redis features)
# For Docker:
REDIS_URL="redis://localhost:6379"
# For Manual installation:
# REDIS_URL="redis://localhost:6379"
# Skip Redis (use database sessions):
# REDIS_URL=""

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
# Generate secret: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
NEXTAUTH_SECRET="change-this-to-a-random-secret-key"

# OAuth Providers
# NOTE: Only providers with valid credentials will be available on the login page
# See OAUTH_SETUP.md for detailed setup instructions

# Google OAuth (Required for Google login)
# Get credentials from: https://console.cloud.google.com/
# Redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Facebook OAuth (Optional)
# Get credentials from: https://developers.facebook.com/
# Redirect URI: http://localhost:3000/api/auth/callback/facebook
FACEBOOK_CLIENT_ID=""
FACEBOOK_CLIENT_SECRET=""

# Twitter/X OAuth (Optional)
# Get credentials from: https://developer.twitter.com/
# Redirect URI: http://localhost:3000/api/auth/callback/twitter
TWITTER_CLIENT_ID=""
TWITTER_CLIENT_SECRET=""

# File Storage (Local Development)
STORAGE_PATH="./storage"
STORAGE_URL="http://localhost:3000/storage"

# MinIO (S3-compatible) - If using Docker MinIO
MINIO_ENDPOINT="localhost"
MINIO_PORT=9000
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin123"
MINIO_BUCKET="maithili-dictionary"
MINIO_USE_SSL=false

# Email Service (Optional for local dev)
# For local development, emails can be logged to console
EMAIL_FROM="noreply@localhost"
EMAIL_PROVIDER="console"
# SendGrid (if using)
SENDGRID_API_KEY=""
# AWS SES (if using)
AWS_SES_REGION=""
AWS_SES_ACCESS_KEY=""
AWS_SES_SECRET_KEY=""

# Search Service (Optional - can add later)
ALGOLIA_APP_ID=""
ALGOLIA_API_KEY=""
ALGOLIA_SEARCH_KEY=""
ALGOLIA_INDEX_NAME="maithili_words"

# Application Configuration
APP_ENV="development"
APP_URL="http://localhost:3000"
APP_NAME="Maithili Dictionary"

# Logging
LOG_LEVEL="debug"

# Rate Limiting (Optional)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# PDF Generation
PDF_GENERATION_TIMEOUT=300000
PDF_STORAGE_PATH="./storage/pdfs"

# Monitoring (Optional)
SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""
